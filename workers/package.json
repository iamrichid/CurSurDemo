{
  "name": "any3mi-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:migrate": "wrangler d1 migrations apply any3mi --local",
    "db:migrate:remote": "wrangler d1 migrations apply any3mi --remote"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}
